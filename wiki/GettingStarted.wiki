#summary Getting Started with FIX Parser
= Getting Started with FIX Parser =

{{{
        FileInputStream fis = new FileInputStream("fix_message.log");
        SimpleFixParser parser = new SimpleFixParser(fis);

        for (int indx = 0; true; indx++) {
            SimpleFixMessage msg = parser.readFixMessage();
            if (null == msg) {
                break;
            }

            // process FIX message

        }

        IoUtils.close(fis);

}}}

= A Wroking Sample =
[http://code.google.com/p/fixparser/source/browse/trunk/src/samples/net/java/fixparser/Sample.java]